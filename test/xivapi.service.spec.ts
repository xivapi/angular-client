import { inject, TestBed } from '@angular/core/testing';



describe('Client tests', () => {

    beforeEach(() => {
        TestBed.configureTestingModule({
            imports: [
                // TODO
            ]
        });
    });

    it('Should be able to call service methods', inject([], () => {
        // TODO
    }));
});
